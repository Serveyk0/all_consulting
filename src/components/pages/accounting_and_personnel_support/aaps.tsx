import React from 'react';
import "./aaps.sass";
import { show } from '../../show';

export const AAPS: React.FC = (  ): JSX.Element =>  
{
    show();
    return (
        <div className="aaps">
            
        </div>
    )
}